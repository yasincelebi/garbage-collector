import React from "react";
import { StyledLoading } from "../styles/StyledTakeAction";

const Loading = () => {
  return (
    <StyledLoading>
      <div className="circle"></div>
    </StyledLoading>
  );
};

export default Loading;
